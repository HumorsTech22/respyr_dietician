import 'package:flutter/material.dart';

class PracticeTestInhaleScreen extends StatelessWidget {
  const PracticeTestInhaleScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.white,
      body: SafeArea(
          child: Column(
            children: [

            ],
          )
      ),
    );
  }
}
